[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "geojson-editor"
version = "0.1.0"
authors = [
    {name = "Yauheni Tabolich", email = "yauheni.tabolich@gmail.com"},
]
license = {text = "MIT"}
requires-python = ">=3.9"
dependencies = [
    "PySide6>=6.4.0",
    "requests>=2.28.0",
    "jsonschema>=4.17.0",
]
